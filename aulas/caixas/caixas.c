#include <stdio.h>

int main()
{
    float altura, largura, profundidade;
    float area_superfície, volume;

    printf("Digite altura");
    scanf("%f", &altura);

    printf("Digite largura");
    scanf("%f", &largura);

    return 0;
}