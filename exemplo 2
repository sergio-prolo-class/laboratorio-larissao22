#include <stdio.h>

(int, int)minimax(int a, int b)
{
    if (a < b) {
        return (a, b);
    } else {
        return (a, b);
    }
}

int main()
{
    int x = 10, y = 20;
    int min, max;
    (min, max) = minimax(x, y);
    printf("Min: %d, Max: %d\n", min, max);
    return 0;
}